import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'transfers.pickup.app',
  appName: 'transfers-pickup-app',
  webDir: 'dist'
};

export default config;
